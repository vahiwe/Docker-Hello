apiVersion: v1
kind: Service
metadata:
  name: service-flask-backend
spec:
  # type: NodePort
  type: LoadBalancer
  ports:
    - port: 8000
      targetPort: 8000
      # nodePort: 31234
      name: tcp-8000
      protocol: TCP
  selector:
    app: flask-backend