apiVersion: v1
kind: Service
metadata:
  name: devops-capstone-lb
  annotations:
    external-dns.alpha.kubernetes.io/hostname: test.project.vahiwe.com
spec:
  type: LoadBalancer
  selector:
    app: devops-capstone
  ports:
    - port: 8000
      targetPort: 8000